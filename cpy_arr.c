#include <stdio.h>
int main()
{
	int i,n;
		printf("Enter the size of the array:");
		scanf("%d",&n); 
	int arr[n];
		printf("Enter the elements in your array: ");
		
			for(i=0;i<n;i++)
			{
			scanf("%d",&arr[i]);
			}
	int a[n];
		printf("Copy of the array: ");
		
			for(i=0;i<n;i++)
			{
			arr[i]==a[n];
			printf("%d",a[n]);
			}
return 0;
}

